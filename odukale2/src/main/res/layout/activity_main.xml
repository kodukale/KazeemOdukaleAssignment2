<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".OdukaleActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <TextView
            android:id="@+id/OdukaleTextView"
            style="@style/mainHeader"
            android:text="@string/mainHeader"
            android:textSize="@dimen/mainHeaderTextSize"
            android:layout_margin="@dimen/mainHeaderMagin"/>

        <ImageView
            android:id="@+id/odukaleImageView"
            android:layout_width="@dimen/logoWidth"
            android:layout_height="@dimen/logoHeight"
            tools:src="@drawable/logo"
            android:layout_gravity="center"/>

        <Button
            android:id="@+id/odukaleEnterButton"
            android:text="@string/enterButton"
            style="@style/enterButton"/>
    </LinearLayout>

</ScrollView>